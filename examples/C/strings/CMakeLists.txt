include_directories(${PROJECT_SOURCE_DIR}/src/public)
include_directories(${PROJECT_SOURCE_DIR}/src)
include_directories(${PROJECT_SOURCE_DIR}/examples/C/strings)
include_directories(${PROJECT_BINARY_DIR}/src/public)
link_directories(${PROJECT_BINARY_DIR}/examples/C/strings)

add_executable(strings_write strings_write.c)
if(MPI_COMPILE_FLAGS)
  set_target_properties(strings_write PROPERTIES COMPILE_FLAGS "${MPI_C_COMPILE_FLAGS}")
endif()
if(MPI_LINK_FLAGS)
  set_target_properties(strings_write PROPERTIES LINK_FLAGS "${MPI_C_LINK_FLAGS}")
endif()
target_link_libraries(strings_write adios ${ADIOSLIB_LDADD} ${MPI_C_LIBRARIES})

#add_executable(strings_read strings_read.c)
#if(MPI_COMPILE_FLAGS)
#  set_target_properties(strings_read PROPERTIES COMPILE_FLAGS "${MPI_C_COMPILE_FLAGS}")
#endif()
#if(MPI_LINK_FLAGS)
#  set_target_properties(strings_read PROPERTIES LINK_FLAGS "${MPI_C_LINK_FLAGS}")
#endif()
#target_link_libraries(strings_read adiosread ${ADIOSREADLIB_LDADD} ${MPI_C_LIBRARIES})

